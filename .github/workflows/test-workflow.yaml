name: List of file availability
on:
  workflow_dispatch:
jobs:
    list-files:
        runs-on: ubuntu-latest
        steps:
        - name: Location path
          run: |
            echo "Current working directory:"
            pwd
        - name: list of files 
          run: |
            echo "List of files in the repository:"
            ls -R
        - name: GIve me the repo name printed
          run: |
            echo "Repository name is: https://githib.com/${{ github.repository}}.git"
# https://githib.com/devopswithcloud/i27-dashboard-b10.git