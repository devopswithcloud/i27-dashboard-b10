name: workflow-2
on: 
  workflow_run: 
    workflows: 
    - workflow-1
    types: 
    - completed
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: Printing 
          run: echo "This workflow is triggered because workflow is completed"